# Return number of words in the string
string = "Python_Class_first_task_is_completed"

# Splitting the word by underscore and counting the length of the words and assigning it to a variable
word_count = len(string.split('_'))

# Printing the word count
print("\n Word Count is:", word_count)
